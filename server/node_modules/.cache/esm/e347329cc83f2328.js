let express,cors,readdirSync,mongoose;_c83‍.w("express",[["default",["express"],function(v){express=v}]]);_c83‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_c83‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_c83‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);const morgan = require("morgan");
require("dotenv").config();





//express app
const app = express();

//db
mongoose.connect("mongodb://127.0.0.1:27017/udemy").then(()=> console.log("*****"))
.catch((e)=>_c83‍.g.console.log("error__", e));

//middle wares
app.use(cors());
app.use(express.json());
app.use(morgan("dev"));

//routes
readdirSync("./routes").map(
  (
    r //executes all the files under the routes folder
  ) => {
    app.use("/api", require(`./routes/${r}`));
  }
);

//port
app.listen(process.env.PORT || 8000, () => {
  console.log("server up...");
});
