let express,cors,readdirSync,mongoose;_301‍.w("express",[["default",["express"],function(v){express=v}]]);_301‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_301‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_301‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);const morgan = require("morgan");
require("dotenv").config();





//express app
const app = express();

// db
mongoose.connect(process.env.DATABASE)
.then(()=> console.log("**DB Connection established**"))
.catch((e)=>_301‍.g.console.log("error in DB: ", e));

//middle wares
app.use(cors());
app.use(express.json());
app.use(morgan("dev"));

//routes
readdirSync("./routes").map(
  (
    r //executes all the files under the routes folder
  ) => {
    app.use("/api", require(`./routes/${r}`));
  }
);

//port
app.listen(process.env.PORT || 8000, () => {
  console.log("server up...");
});
